version: 2.1

jobs:
  build:
    docker:
      - image: node
    steps:
      - checkout
      - run: echo "this is the build job"
      - run: npm run build:js
      - run: npm publish
  test:
    docker:
      - image: node
    steps:
      - checkout
      - run: echo "this is the test job"
      - run: npm run test:ci

workflows:
  build_and_test:
    jobs:
      - test
      - build
